First, to transpose ${{ A }}$ make the first row the first column, the second row the second column, and so on.

Find the entry in row ${{r1 + 1}}$ and column ${{c1 + 1}}$, then multiply it with the entry in row ${{r2 + 1}}$ and column ${{c2 + 1}}$.